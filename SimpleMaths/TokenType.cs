namespace SimpleMaths
{
    public enum TokenType
    {
        NONE,  //Default      
        ASSIGN,
        PLUS,
        MINUS,
        ASTERISK,
        SLASH,       
        NAME,
        EOF,
    }
}