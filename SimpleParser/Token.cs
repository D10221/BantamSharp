


namespace SimpleParser
{
    public class Token
    {
        public static IToken<T> New<T>(T tokenType, string text)
        {
            return new Token<T>(tokenType, text);
        }

        public static IToken<T> New<T>(T tokenType, char c)
        {
            return New(tokenType, c.ToString());
        }
    }
    /// <summary>
    /// A simple token class. These are generated by Lexer and consumed by Parser.
    /// </summary>
    public class Token<TTokenType> : IToken<TTokenType>
    {
        public Token(TTokenType type, string text)
        {
            TokenType = type;
            _text = text;
        }

        public TTokenType TokenType { get; }

        public string GetText()
        {
            return _text;
        }

        public override string ToString()
        {
            return _text;
        }

        private readonly string _text;


        public static IToken<TTokenType> Empty()
        {
            return new Token<TTokenType>(default(TTokenType), null);
        }

        public bool HasValue
        {
            get { return !Equals(TokenType, default(TTokenType)); }
        }

        public bool IsEmpty
        {
            get { return Equals(TokenType, default(TTokenType)); }
        }
    }
}